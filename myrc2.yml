apiVersion: v1
kind: ReplicationController
metadata: 
  name: myjava-rc
spec:
  replicas: 3
  selector: 
    app: myjava-pod
    version: 2.7.1
  template:
  metadata:
   labels:
    app: myjava-pod
    version: 2.7.1
  spec:
   containers:
   - name: myjava-container
     image: 
     ports:
     - containerPort: 8080
     