apiVersion: v1
kind: Service
metadata:
  name: myjava-svc
  labels:
     app: myjava
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30001
    protocol: TCP
  selector:
      app: myajava

# -p 30001:8080
